```
/cmatrix # history
   0 hostname
   1 git clone https://github.com/spurin/cmatrix.git
   2 apk add git
   3 git clone https://github.com/spurin/cmatrix.git
   4 history
   5 ls -l
   6 apk add autoconf
   7 autoreconf -i
   8 apk add automake
   9 autoreconf -i
  10 ls configure.ac configure.in
  11 autoscan
  12 mv configure.scan configure.ac
  13 sudo apt-get install autoconf automake
  14 apt-get install autoconf automake
  15 autoreconf -i
  16 echo $?
  17 ./configure LDFLAGS="-static"
  18 apk add alpine-sdk
  19 ./configure LDFLAGS="-static"
  20 apk add ncureses-dev ncurses-static
  21 apk add ncurses-dev ncurses-static
  22 mkdir -p /usr/lib/kbd/consolefonts /usr/share/consolefonts
  23 ./configure LDFLAGS="-static"
  24 make
  25 make all-am
  26 ls Makefile makefile GNUmakefile
  27 ./configure
  28 autoreconf -i
  29 ls Makefile
  30 make
  31 ./configure
  32 ls Makefile
  33 find . -name Makefile
  34 make clean
  35 rm -f config.log config.status
  36 autoreconf -i
  37 ./configure
  38 cat config.log
  39 ls -ld .
  40 apk add build-base autoconf automake libtool
  41 autoreconf -i
  42 ./configure
  43 ./configure LDFLAGS="-static"
  44 make
  45 apk add git autoconf automake alpine-sdk ncurses-dev ncurses-static ncurses-libs
  46 ./configure LDFLAGS="-static"
  47 make
  48 mkdir -p /usr/lib/kbd/consolefonts /usr/share/consolefonts
  49 ./configure LDFLAGS="-static"
  50 make
  51 apk add automake
  52 autoreconf -i
  53 autoupdate
  54 ./configure LDFLAGS="-static"
  55 apk add ncurses-dev ncurses-static
  56 ./configure LDFLAGS="-static"
  57 make
  58 history
  59 ls
  60 ls -lrt
  61 cd cmatrix
  62 ls
  63 autoreconf -i
  64 autoupdate
  65 apk add autoconf
  66 autoreconf -i
  67 apk add automake
  68 autoreconf -i
  69 ls
  70 ./configure LDFLAGS="-static"
  71 apk add alpine-sdk
  72 ./configure LDFLAGS="-static"
  73 apk add ncurses-dev ncurses-static
  74 ./configure LDFLAGS="-static"
  75 make
  76 ls -l ./cmatrix
  77 ./cmatrix
  78 history
  ```